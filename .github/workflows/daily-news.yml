name: Daily Stock Market News Email

on:
  schedule:
    - cron: '40 13 * * 1-5'
  workflow_dispatch:

jobs:
  send-news:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-python@v5
      with:
        python-version: '3.12'
    - run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    - run: python main.py
      env:
        ALPHA_VANTAGE_API_KEY: "YN6IS40RBNSQJWT9"
        SENDGRID_API_KEY: "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"
        EMAIL_SENDER: "coda0418@hotmail.com"
        EMAIL_RECIPIENT: "coda0418@hotmail.com"
